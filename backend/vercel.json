{
  "version": 2,

  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://feedtrack-backend.vercel.app/api/:path*"
    }
  ],

  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "builds": [
    { "src": "./api/index.js", "use": "@vercel/node"}
  ]
}
