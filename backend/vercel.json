{
  "version": 2,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/api" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "builds": [
    { "src": "./api/index.js", "use": "@vercel/node"}
  ]
}